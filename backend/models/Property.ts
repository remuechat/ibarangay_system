export interface Property {
  propertyId: string;
  name: string;
  category: string;
  description: string;
  quantity: number;
  condition: string;
  location: string;
  createdAt: string;
  updatedAt: string;
}
